#include<bits/stdc++.h>
using namespace std;
int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);

    int n;
    cin>>n;
    while(n--)
    {
        long long int a;
        cin>>a;
        cout<<1<<" "<<(a-1)<<"\n";
    }


    return 0;
}